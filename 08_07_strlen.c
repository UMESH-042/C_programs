#include<stdio.h>
#include<string.h>

int main(){
    char *st ="Umesh";
    int a=strlen(st);//This function is used to count the number of characters excluding the null('\0') character
    printf("The length of string st is %d\n",a);
    return 0;
}
// Q.Write a function using loop(while loop) to calculate the length of string ?